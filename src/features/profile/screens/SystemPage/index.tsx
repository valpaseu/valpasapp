import { Button, View } from "react-native";
import React from "react";

const SystemPage = () => {
  return (
    <View>
      <Button
        title="ddd"
        onPress={() => {
          console.log("dddd");
        }}
      />
    </View>
  );
};

export default SystemPage;
